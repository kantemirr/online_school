"""Тексты теории уроков (Markdown) для сидов каталога.

Вынесено из seeds_content.py, чтобы каталог оставался читаемой структурой,
а длинные тексты жили отдельно. Ключи — стабильные слаги «курс_урок».
"""

THEORY: dict[str, str] = {
    # ── Python с нуля для детей (демо-курс) ──────────────────────────────────
    "py_intro": """# Что такое программирование

Представь, что у тебя есть очень исполнительный, но совсем не догадливый помощник —
**компьютер**. Он умеет считать с невероятной скоростью, рисовать, играть музыку и
запускать игры. Но есть одна хитрость: сам по себе он не знает, что делать. Ему нужно
**объяснить каждый шаг**. Вот это объяснение и называется *программированием*.

> **Программирование** — это способ дать компьютеру понятные пошаговые инструкции,
> чтобы он выполнил то, что мы задумали.

## Компьютер выполняет инструкции

Компьютер похож на робота из сказки: он делает **ровно то, что ему сказали** — не больше
и не меньше. Если в инструкции ошибка, он честно выполнит ошибку. Поэтому программист
должен думать чётко и по порядку.

Сравни две инструкции «приготовить бутерброд»:

- 🙂 *Возьми хлеб. Намажь масло. Положи сыр.* — понятно и по шагам.
- 🤔 *Сделай бутерброд.* — а как именно? Робот растеряется.

Компьютеру нужен **первый** вариант — подробный и пошаговый.

## Алгоритм — это рецепт для компьютера

Последовательность шагов, которая решает задачу, называется **алгоритмом**. Это как
рецепт в кулинарной книге или инструкция к конструктору.

Алгоритм «перейти дорогу»:

1. Подойти к переходу.
2. Посмотреть налево и направо.
3. Дождаться зелёного света.
4. Убедиться, что машины остановились.
5. Перейти дорогу.

Любая программа — это алгоритм, записанный на языке, который понимает компьютер.

## Где живёт программирование

Программы окружают нас повсюду. Их написали программисты:

- 🎮 **Игры** — от мобильных до больших компьютерных миров.
- 🌐 **Сайты и приложения** — YouTube, карты, мессенджеры.
- 🤖 **Роботы и умные устройства** — пылесосы, колонки, машины.
- 🚀 **Наука и космос** — расчёты для ракет и телескопов.

Всё это — результат того, что кто-то однажды объяснил компьютеру, что делать.

## На чём «разговаривают» с компьютером

Чтобы давать инструкции, придумали **языки программирования**. Их много, и у каждого
свой характер:

- **Scratch** — язык из ярких блоков-пазлов, с него удобно начинать.
- **Python** — наш язык в этом курсе. Команды на нём короткие и читаются почти как
  обычный английский, поэтому он отлично подходит для первых шагов.

## Самая первая команда

В Python вывести надпись на экран можно одной строчкой:

```python
print("Привет, мир!")
```

Команда `print` означает «напечатай», а в кавычках мы пишем то, что хотим показать.
Запусти такую программу — и компьютер послушно выведет: `Привет, мир!`

## Что развивает программирование

Когда ты пишешь код, ты тренируешь полезные суперспособности:

- **Логику** — выстраивать шаги по порядку.
- **Внимание** — замечать мелочи (компьютер не прощает опечаток).
- **Творчество** — придумывать свои игры и проекты.
- **Терпение** — искать и исправлять ошибки. Это нормально, ошибаются все!

## Что дальше

На следующих уроках ты познакомишься с командой `print`, научишься хранить данные в
**переменных** и решишь первую настоящую задачу с автоматической проверкой. Поехали! 🚀
""",

    "py_print": """# Первая программа: print

Пора написать первую настоящую программу! В Python за вывод текста на экран отвечает
команда **`print`** (по-английски — «напечатать»).

## Как устроена команда

```python
print("Привет, мир!")
```

Разберём по частям:

- `print` — имя команды (функции);
- круглые скобки `()` — в них мы передаём то, что надо показать;
- кавычки `"..."` — внутри них находится **текст** (на языке программистов — *строка*).

Запусти программу — и компьютер выведет: `Привет, мир!`

## Можно выводить несколько строк

Каждая команда `print` печатает с новой строки:

```python
print("Меня зовут Кодик")
print("Я учу детей программировать")
print("Поехали!")
```

На экране появится три строки по очереди.

## Текст и числа

В кавычках может быть любой текст, а числа можно печатать и без кавычек:

```python
print("2 + 2 =")
print(4)
```

## Частые ошибки

- 🔴 Забыли закрыть кавычку: `print("Привет)` — программа не запустится.
- 🔴 Русская буква вместо команды: `принт(...)` — Python знает только `print`.
- 🟢 Правильно: открыли и закрыли и кавычки, и скобки.

## Что дальше

Ты научился показывать текст на экране. Дальше научимся **запоминать** значения в
переменных — это как коробочки с подписями, в которых хранятся данные.
""",

    "py_vars": """# Переменные

Представь коробочку с подписью, в которую можно что-то положить и потом достать. В
программировании такая коробочка называется **переменной**.

## Создаём переменную

```python
name = "Алиса"
age = 10
```

Слева — **имя** переменной, справа от знака `=` — её **значение**. Теперь компьютер
помнит, что `name` — это «Алиса», а `age` — это число 10.

> Знак `=` здесь означает не «равно», а «положи в коробочку». Читается как «присвоить».

## Используем переменную

```python
name = "Алиса"
print("Привет,")
print(name)
```

Программа выведет приветствие и имя из переменной.

## Значение можно менять

```python
score = 0
score = score + 10
print(score)   # 10
```

Сначала в `score` было 0, потом мы прибавили 10 — стало 10. Так в играх растут очки.

## Имена переменных

- можно буквы, цифры и знак `_` (но не начинать с цифры);
- лучше понятные имена: `speed`, `player_name`, а не `a` и `x1`;
- большие и маленькие буквы различаются: `Age` и `age` — разные переменные.

## Что дальше

Переменные умеют хранить числа — а значит, с ними можно считать. На следующем уроке
займёмся **арифметикой**, а затем решишь задачу с автоматической проверкой!
""",

    "py_arith": """# Арифметика

Компьютер — отличный калькулятор. В Python есть все привычные действия с числами и
ещё немного больше.

## Основные операции

```python
print(2 + 3)   # сложение → 5
print(7 - 4)   # вычитание → 3
print(5 * 6)   # умножение → 30
print(8 / 2)   # деление → 4.0
```

## Полезные «секретные» операции

```python
print(17 // 5)  # целая часть от деления → 3
print(17 % 5)   # остаток от деления → 2
print(2 ** 10)  # степень: 2 в 10-й → 1024
```

Остаток `%` очень помогает: например, число чётное, если `n % 2 == 0`.

## Порядок действий

Как и в математике, сначала умножение и деление, потом сложение и вычитание. Скобки
меняют порядок:

```python
print(2 + 3 * 4)    # 14
print((2 + 3) * 4)  # 20
```

## Считаем с переменными

```python
price = 100
count = 3
total = price * count
print(total)   # 300
```

## Что дальше

Теперь ты умеешь и хранить данные, и считать. Самое время решить **первую настоящую
задачу** — компьютер проверит твой код автоматически. Удачи! 💪
""",

    # ── Python: продолжаем ───────────────────────────────────────────────────
    "pyc_if": """# Условия: if / else

Программы становятся умными, когда умеют **принимать решения**. За это отвечает
конструкция `if` («если»).

## Если… то…

```python
age = 12
if age >= 10:
    print("Можно начинать курс!")
```

Двоеточие `:` и отступ показывают, какой код относится к условию. Если условие верно
(`True`), код внутри выполняется; если нет — пропускается.

## Иначе — else

```python
temperature = 5
if temperature > 15:
    print("Тепло, идём гулять")
else:
    print("Холодно, берём куртку")
```

`else` («иначе») срабатывает, когда условие не выполнено.

## Несколько вариантов — elif

```python
mark = 4
if mark == 5:
    print("Отлично!")
elif mark == 4:
    print("Хорошо")
else:
    print("Старайся ещё")
```

## Операторы сравнения

| Оператор | Значение |
|---|---|
| `==` | равно |
| `!=` | не равно |
| `>` `<` | больше / меньше |
| `>=` `<=` | больше-равно / меньше-равно |

> Не путай `=` (положить в переменную) и `==` (сравнить). Это частая ошибка новичков!

## Что дальше

Попробуй сам: определи, чётное число или нечётное, с помощью остатка `% 2`. Эту задачу
проверит песочница.
""",

    "pyc_for": """# Цикл for

Что делать, если действие нужно повторить много раз? Не писать же сто строк! На помощь
приходит **цикл** `for`.

## range — диапазон чисел

```python
for i in range(5):
    print(i)
```

Выведет числа `0 1 2 3 4` — пять раз. Переменная `i` на каждом шаге принимает следующее
значение из диапазона.

## Повторяем действие

```python
for i in range(3):
    print("Привет!")
```

Напечатает приветствие три раза.

## Копим результат

Часто в цикле что-то **накапливают** — например, сумму:

```python
total = 0
for i in range(1, 6):   # числа 1, 2, 3, 4, 5
    total = total + i
print(total)            # 15
```

`range(1, 6)` даёт числа от 1 до 5 (правая граница не входит).

## Где это нужно

- посчитать сумму или произведение чисел;
- нарисовать узор из повторяющихся фигур;
- обойти все буквы в слове или предметы в списке.

## Что дальше

Закрепим цикл на задаче: посчитай сумму всех чисел от 1 до N. Дальше — знаменитая игра
FizzBuzz!
""",

    "pyc_fizz": """# FizzBuzz

**FizzBuzz** — классическая задача, которую любят давать даже на собеседованиях. На ней
удобно потренировать условия внутри цикла.

## Правила

Для чисел от 1 до N выводим:

- `Fizz` — если число делится на 3;
- `Buzz` — если делится на 5;
- `FizzBuzz` — если делится и на 3, и на 5;
- само число — в остальных случаях.

## Главный инструмент — остаток

Число делится на 3 без остатка, если `n % 3 == 0`:

```python
n = 9
if n % 3 == 0:
    print("делится на 3")
```

## Порядок условий важен

Сначала проверяем «и на 3, и на 5», иначе `FizzBuzz` никогда не сработает:

```python
if n % 3 == 0 and n % 5 == 0:
    print("FizzBuzz")
elif n % 3 == 0:
    print("Fizz")
elif n % 5 == 0:
    print("Buzz")
else:
    print(n)
```

Слово `and` означает «и» — оба условия должны быть верны одновременно.

## Что дальше

Реши FizzBuzz для одного числа в песочнице, а затем перейдём к работе со **строками**.
""",

    "pyc_strings": """# Работа со строками

**Строка** — это текст: слово, фраза или даже целая книга. В Python со строками удобно
работать множеством способов.

## Длина строки

```python
word = "программа"
print(len(word))   # 9
```

`len` возвращает количество символов.

## Обращение по индексу

У каждого символа есть номер (индекс), начиная с **0**:

```python
word = "Кодик"
print(word[0])    # К
print(word[1])    # о
```

## Срезы

Можно взять кусочек строки `строка[начало:конец]`:

```python
word = "программирование"
print(word[0:7])   # програм
```

## Переворот строки

Хитрый срез `[::-1]` переворачивает строку задом наперёд:

```python
word = "привет"
print(word[::-1])  # тевирп
```

## Соединение строк

```python
name = "Алиса"
print("Привет, " + name + "!")
```

Знак `+` склеивает строки вместе.

## Что дальше

Проверь себя: напиши программу, которая переворачивает введённое слово. Песочница
оценит результат автоматически.
""",

    # ── Python: игры с черепашкой ────────────────────────────────────────────
    "turtle_first": """# Первые шаги черепашки

В Python есть волшебный модуль **`turtle`** — это черепашка, которая ползает по экрану и
оставляет за собой линию. Управляя ею командами, можно рисовать настоящие картины!

## Подключаем черепашку

```python
import turtle
t = turtle.Turtle()
```

Теперь `t` — наша черепашка, и ей можно отдавать команды.

## Движение

```python
t.forward(100)   # ползёт вперёд на 100 шагов
t.right(90)      # поворот направо на 90 градусов
t.left(45)       # поворот налево на 45 градусов
```

Черепашка рисует линию там, где проползла.

## Поднять и опустить перо

Чтобы переместиться **без линии**, перо поднимают:

```python
t.penup()        # перо вверх — не рисуем
t.forward(50)
t.pendown()      # перо вниз — снова рисуем
```

## Цвет и толщина

```python
t.color("red")     # цвет линии
t.pensize(3)       # толщина
```

## Углы — это важно

Поворот измеряется в **градусах**: полный круг — 360°, прямой угол — 90°. Это пригодится,
когда будем рисовать фигуры.

## Что дальше

На следующем уроке нарисуем квадрат с помощью цикла и посчитаем его периметр.
""",

    "turtle_square": """# Рисуем квадрат

У квадрата четыре одинаковые стороны и четыре прямых угла. Значит, рисунок — это четыре
раза «вперёд и поворот на 90°».

## Без цикла — длинно

```python
t.forward(100)
t.right(90)
t.forward(100)
t.right(90)
t.forward(100)
t.right(90)
t.forward(100)
t.right(90)
```

Работает, но слишком много повторов.

## С циклом — красиво

```python
for i in range(4):
    t.forward(100)
    t.right(90)
```

Четыре повтора — и квадрат готов! Поменяй `range(4)` на `range(3)` — получится
треугольник (только угол поворота должен быть 120°).

## Периметр квадрата

Периметр — это длина всех сторон вместе. У квадрата все стороны равны, поэтому:

```python
side = 100
perimeter = side * 4
print(perimeter)   # 400
```

## Идея для проекта

Нарисуй домик: квадрат — стены, треугольник — крыша. Добавь цвет и толщину линии. Чем
больше деталей — тем интереснее!

## Что дальше

Посчитай периметр квадрата в песочнице, а потом загрузи свой рисунок черепашкой как
проект — преподаватель его оценит.
""",

    # ── Алгоритмы на Python ──────────────────────────────────────────────────
    "algo_max": """# Максимум и минимум

**Алгоритм** — это чёткая последовательность шагов для решения задачи. Начнём с простого:
найти из двух чисел большее.

## Сравнение «в лоб»

```python
a = 3
b = 8
if a > b:
    print(a)
else:
    print(b)
```

Если `a` больше `b`, выводим `a`, иначе — `b`.

## Готовые функции

В Python уже есть `max` и `min`:

```python
print(max(3, 8))   # 8
print(min(3, 8))   # 3
```

Они работают и с несколькими числами: `max(4, 9, 2)` вернёт 9.

## Считываем числа

Чтобы получить числа от пользователя, используем `input` и `int`:

```python
a, b = input().split()
a = int(a)
b = int(b)
print(max(a, b))
```

`split()` разбивает введённую строку по пробелам, а `int` превращает текст в число.

## Что дальше

Реши задачу «Максимум из двух» в песочнице, а затем перейдём к **факториалу** — там
понадобится цикл.
""",

    "algo_fact": """# Факториал

**Факториал** числа n (записывается `n!`) — это произведение всех чисел от 1 до n:

```
5! = 1 · 2 · 3 · 4 · 5 = 120
```

Факториалы быстро растут и часто встречаются в задачах на перебор и вероятности.

## Считаем циклом

Накапливаем произведение в переменной:

```python
n = 5
result = 1
for i in range(1, n + 1):
    result = result * i
print(result)   # 120
```

Начинаем с `1` (не с 0 — иначе всё умножение обнулится) и умножаем на каждое число до n.

## Особый случай

По договорённости `0! = 1`. Наш цикл это учитывает: при `n = 0` тело цикла не выполнится,
и останется начальная `1`.

## Проверь себя

- `3!` = ? (ответ: 6)
- `4!` = ? (ответ: 24)

## Что дальше

Напиши программу-факториал в песочнице. Дальше научимся определять **простые числа**.
""",

    "algo_prime": """# Простое число

**Простое число** делится без остатка только на 1 и на само себя. Например: 2, 3, 5, 7,
11, 13. А вот 8 простым не является — оно делится ещё на 2 и на 4.

## Как проверить

Число `n` простое, если у него нет делителей среди чисел от 2 до n−1:

```python
n = 7
is_prime = True
for d in range(2, n):
    if n % d == 0:
        is_prime = False
if is_prime and n > 1:
    print("да")
else:
    print("нет")
```

Если нашёлся хоть один делитель (`n % d == 0`), число составное.

## Важные мелочи

- 1 — **не** простое число (по определению).
- 2 — самое маленькое простое и единственное чётное простое.

## Чуть быстрее

Достаточно проверять делители до квадратного корня из n — так алгоритм работает быстрее
на больших числах. Но для начала хватит и простого перебора.

## Что дальше

Сначала ответь на квиз, а затем реши задачу «Простое ли число» в песочнице.
""",

    "algo_vowels": """# Подсчёт гласных

Частая задача — посчитать в слове буквы определённого вида. Потренируемся на **гласных**.

## Перебираем буквы

Цикл `for` умеет проходить по строке буква за буквой:

```python
word = "привет"
for letter in word:
    print(letter)
```

## Считаем нужные буквы

Заводим счётчик и увеличиваем его, когда буква — гласная:

```python
word = "программа"
vowels = "аеёиоуыэюя"
count = 0
for letter in word:
    if letter in vowels:
        count = count + 1
print(count)   # 3
```

Оператор `in` проверяет, есть ли буква в строке гласных.

## Полезно знать

- регистр важен: `А` и `а` для компьютера разные — можно привести слово к нижнему
  регистру методом `word.lower()`;
- так же можно считать согласные, цифры или пробелы — меняется лишь условие.

## Что дальше

Реши задачу «Сколько гласных» в песочнице — и поздравляю, базовые алгоритмы освоены! 🎉
""",

    # ── Scratch: первые игры ─────────────────────────────────────────────────
    "scr1_sprites": """# Сцена и спрайты

**Scratch** — это среда, где программы собирают не из текста, а из ярких **блоков**,
которые соединяются, как пазлы. Идеально, чтобы начать!

## Главные слова

- **Спрайт** — это персонаж или предмет: котик, мячик, ракета. С ним и происходят все
  действия.
- **Сцена** — это фон, по которому двигаются спрайты: космос, лес, комната.
- **Костюм** — внешний вид спрайта. У одного спрайта может быть несколько костюмов, и
  если их быстро менять — получится анимация.

## Координаты

Сцена — как карта с координатами **x** (влево-вправо) и **y** (вверх-вниз). Центр — это
точка `x = 0, y = 0`. Команда «перейти в x: 100 y: 50» мгновенно переносит спрайт.

## Как собрать программу

1. Выбираем спрайт.
2. Перетаскиваем нужные блоки в область скриптов.
3. Соединяем их сверху вниз — получается последовательность действий.
4. Нажимаем зелёный флажок — программа запускается.

## Что дальше

Дальше научимся **запускать** программу по событию и **двигать** спрайт по сцене.
""",

    "scr1_move": """# Движение и события

Чтобы программа ожила, ей нужен **старт** (событие) и **действия** (движение).

## События — с чего всё начинается

Жёлтые блоки событий запускают скрипт:

- **«когда нажат зелёный флажок»** — самый частый старт программы;
- «когда нажата клавиша …» — реакция на кнопку;
- «когда спрайт нажат» — реакция на клик.

## Блоки движения

Синие блоки двигают спрайт:

- **«идти 10 шагов»** — вперёд на 10 шагов;
- «повернуть на 15 градусов» — поворот;
- «перейти в x: … y: …» — телепорт в точку;
- «если касается края — оттолкнуться» — чтобы не убежать со сцены.

## Простой пример (по-блочно)

```
когда нажат зелёный флажок
  всегда
    идти 10 шагов
    если касается края — оттолкнуться
```

Спрайт будет бесконечно ездить от края до края.

## Событие ≠ движение

Запомни разницу: **событие** говорит «когда начать», а **движение** — «что делать».
Сначала событие, потом действия под ним.

## Что дальше

Соберём настоящую игру: добавим **циклы** и **условия**.
""",

    "scr1_loop": """# Цикл и условие

Игры повторяют действия снова и снова и постоянно что-то проверяют. За это отвечают
**циклы** и **условия**.

## Циклы — повторение

- **«повторить 10»** — выполнить блоки внутри ровно 10 раз;
- **«всегда»** — повторять бесконечно (пока игра идёт).

```
повторить 4
  идти 50 шагов
  повернуть на 90 градусов
```

Этот скрипт нарисует движение по квадрату.

## Условия — проверка

Блок **«если … то»** выполняет действия, только когда условие верно:

```
всегда
  если касается «яблоко»
    изменить «счёт» на 1
```

## Из чего складывается игра

- **движение** героя (стрелками или само);
- **проверки**: касание предмета, выход за край, столкновение;
- **счёт** — переменная, которая растёт за правильные действия.

## Что дальше

Пора собрать свою первую игру и загрузить её как проект!
""",

    "scr1_project": """# Проект: своя игра

Время сделать собственную игру! Предлагаем простую, но увлекательную идею: **герой ловит
падающие предметы**.

## Что понадобится

1. **Спрайт-герой** — двигается влево-вправо по нижней части сцены.
2. **Спрайт-предмет** (например, яблоко) — падает сверху вниз.
3. **Переменная «счёт»** — считает пойманные предметы.

## Логика по шагам

```
Герой:
  всегда
    если нажата стрелка влево — идти влево
    если нажата стрелка вправо — идти вправо

Яблоко:
  всегда
    перейти в случайную точку сверху
    опускаться вниз
    если касается героя
      изменить «счёт» на 1
```

## Как сделать интереснее

- ускоряй падение, когда счёт растёт;
- добавь звук при поимке;
- сделай «жизни» — если предмет упал мимо, отнимай одну.

## Сдача проекта

Сохрани проект и загрузи файл `.sb3` или ссылку на него — преподаватель посмотрит и
оставит отзыв с баллами. Удачи, геймдизайнер! 🎮
""",
}
